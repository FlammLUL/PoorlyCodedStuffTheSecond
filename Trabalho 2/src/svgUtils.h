#ifndef SVGUTILS_H_
#define SVGUTILS_H_

/* Tratamento de SVGs */
void makeSVG(Environment*, Queue*);
void makeLinesSVG(FILE*, Environment*, Queue*);

/* Utilidades, que nem foram utilizadas at all */
void removeCharFromString(char**, char);
#endif

